# BrainRun bounded acquisition and metadata guards

Compressed-only acquisition is running; do not restart while session44735 is live. Full download requires published MD5 and previously verified ZIP directory equality before completion. No member extraction.

BSON reader skips non-allowlisted values by framing, including opaque nested poison, with16MiB document bound.52 generated tests cover metadata framing, tagged identities, truncation/duplicates and deterministic disjoint capped roles. These tests are infrastructure evidence, not biometric validation.

Identity role script is prepared but has not executed. It will read only device_id/user_id from devices.bson after acquisition verification. Active caps120fit/40selection/40calibration/60DEV are hash-selected without behavioral eligibility filtering. Test and unused identities stay sealed. All behavioral decoding remains pending a separate TRAIN schema protocol.
