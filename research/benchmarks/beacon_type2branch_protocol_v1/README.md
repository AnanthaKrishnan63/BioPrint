# Paired Type2Branch transfer protocol

Protocol frozen before new BEACON encoder inference. Keeps the prior paired windows, including short inputs, and selects fusion on reserved TRAIN identities by FRR at 1% FAR, then EER, then smaller C. Source encoders stay frozen.

Generated adapter and selection tests: 25 passed. Source hashes and original identity groups independently checked. No dataset observations read during this protocol/implementation step. TRAIN feature preparation, actual inference, fusion fitting/calibration, DEV validation and API replay remain pending. This is not an all-feature result or an exact published SOTA reproduction.
