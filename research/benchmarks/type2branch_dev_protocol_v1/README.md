# Cross-session DEV extension

Frozen before new session2 payload decoding. All79active identities use S1windows15–19 for enrollment and S2windows0–9 for probes. This is an explicit allocation extension of the earlier same-session scoring contract, which is preserved. Enrollment windows are excluded from parameter fitting, model selection, and threshold calibration; some fitting-session windows were previously inspected for data-quality diagnostics.

No cohort reduction or window replacement: incomplete/invalid data yields infeasibility. Population, adapter, model, and global threshold remain frozen. Expected790genuine/61,620impostor comparisons; report representation-fit47, checkpoint-selection-exposed16, and threshold-calibration-exposed16cohorts. None is an untouched identity test; priorDEV exposure is retained as a limitation.

Sixteen combined generated scorer/allocation tests pass. Actual DEV features, metrics and API replay remain pending training/calibration completion.
