name: bigidea
channels:
  - conda-forge
dependencies:
  - python=3.12
  - fastapi
  - uvicorn
  - evdev          # reads /dev/input, for verify/evdev_logger.py
  - numpy          # bioprint scoring
  - pytest         # bioprint tests
  - httpx          # fastapi TestClient
  # added as the research moves into analysis:
  # - pandas
  # - scikit-learn
  # - jupyterlab
