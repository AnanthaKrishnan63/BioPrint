# Frozen short-capture DEV result

All79active identities were eligible:78used100events and one used25events. The frozen mixed epoch4/500-update encoder and separately calibrated exact-length thresholds produced563false accepts among6162impostor claims (FAR9.13664%) and24false rejects among79genuine claims (FRR30.37975%). Pooled raw-score and margin discrete EER were13.93216%. Direct acceptance was55/79; the other24require additional verification.

The empirical1% TRAIN calibration FAR target did not transfer to this cross-session DEV protocol. No threshold was adjusted after validation. Every saved score/report metric and decision was independently checked. One short-capture identity supplies too little evidence for a short-session performance claim; its genuine probe was not accepted.

This is a disclosed follow-up with a different capture budget than the preserved failed1,000-event protocol. Identity cohorts were exposed through model fitting, checkpoint selection or threshold calibration, and this DEV split had prior experiments. One probe per identity and dependent all-claim comparisons limit precision. These are keystroke-only results, not all-feature fusion or final-test performance. Actual API replay is recorded separately.
