# Generated checkpoint restore audit

Saved initialization checkpoint restored with NumPy1.26.4/TensorFlow CPU. Verified optimizer iteration0. Saved71model variables, deliberately zeroed them, restored all bit-exactly, and repeated generated2x100x5inference:2x256embeddings with maximum absolute difference0. Runtime3.3731s. No dataset observations or model-selection decision. Trained-checkpoint restore still pending.
