# Fitting-only digraph continuity diagnostic

Frozen47fitting identities, original session1 only. Selection/calibration/DEV
and sealed-test timing/key suffixes never reach the parser. Source file and
role hashes are checked before the scan.132,428rows were inspected.

Conditional on conventional D/U definitions, HT+UD=DD and HT+UU=DU.
The next event hold is DU-DD; adjacent rows should share key2/nextkey1 and
that inferred hold. All decoded within-row equations and130,205checked
adjacencies pass the fixed1e-5second diagnostic tolerance. This is not proof
of unique original event continuity: absolute timestamps/task boundaries and
recorder quantization are not established. Decimal precision does not prove
timestamp accuracy. The tolerance was fixed before this scan, not optimized.

There are57undecodable rows and2,114nonpositive DD values. This first diagnostic
does not separate missing/malformed/nonfinite failures or zero/negative DD;
these require a fitting-only follow-up before setting the actual event policy.
Negative UD/UU are permitted as overlap/reversed release order. Invalid rows
split runs; no imputation, reordering, clipping or cross-gap concatenation.
Thirty of47fitting identities have at least15candidate nonoverlapping100-row
windows under this strict diagnostic. All47remain in the report; no cohort
was selected for training or replacement identities chosen.

Three generated role/continuity/boundary tests pass. This is data validation,
not extended-feature synthesis validation, model fitting or recognition metrics.
