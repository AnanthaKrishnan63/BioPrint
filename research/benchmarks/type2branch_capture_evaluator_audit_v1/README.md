# Capture DEV evaluator audit

Prepared actual source-encoder inference, exact five-window gallery assembly, all-account mean-Euclidean scoring and length-specific calibrated decisions. Prerequisites, source/input hashes, checkpoint binding and optimizer iterations are checked before scoring. The actual premature invocation rejected missing DEV preparation before output creation/model loading.

Metrics explicitly separate eligible conditional FAR/FRR from whole-cohort additional verification. Coverage includes identity reasons, TRAIN-role fractions, exact-length distribution and ineligible counts. Both raw-score and threshold-margin discrete EER are reported, because per-length thresholds differ. Empty scored cohorts return no recognition metric rather than artificial zero FAR. Cohort reports distinguish full-claim and within-cohort comparisons.

Generated tests cover exact gallery ordering/missing or duplicate windows, incomplete-preparation rejection, ties, length thresholds, missing captures, all-ineligible captures, partial role coverage, empty roles, and raw-score versus margin EER. Real DEV inference and API replay remain pending.
