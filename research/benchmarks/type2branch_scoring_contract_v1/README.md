# Type2Branch scoring contract

Frozen before calibration or DEV scoring. Source mean Euclidean gallery distance is preserved with reversed score sign. Five chronological enrollment windows and ten probes/person; all other identities supply impostor claims. Global threshold targets empirical1%FAR using only innerTRAIN calibration, with float64 tie-safe inclusive comparisons.

Eleven generated tests pass, including105scores compared with the actual author function, chronological disjoint gallery/probes, malformed inputs, ties and2400-score acceptance budget. Source evaluation script5197bytes was fetched at the already pinned commit and verified by publisherGitblobSHA1 and localSHA256.

The source randomly reshuffles galleries and samples200impostor users; this small-cohort chronological protocol is an explicit adaptation. EER will be a discrete closest-point diagnostic, not a deployed threshold. No actual embeddings or calibration/dev/test scores read in this stage. Training remains live separately.
